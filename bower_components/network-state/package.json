{
  "name": "network-state",
  "version": "0.1.1",
  "license": "Apache-2.0 OR CC-BY-4.0",
  "description": "A simple element to detect and notify online/offline swtatus",
  "devDependencies": {
    "polymer-cli": "^0.16.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "private": false,
  "main": [
    "network-state.html"
  ],
  "author": "The Advanced REST client authors <arc@mulesoft.com>",
  "scripts": {
    "serve": "node_modules/.bin/polymer serve",
    "lint": "node_modules/.bin/polymer lint network-state.html",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"network-state:local-test\"",
    "test": "node_modules/.bin/polymer test",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/network-state.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/network-state/issues",
    "email": "arc@mulesoft.com"
  }
}