{
  "name": "json-table",
  "version": "0.1.14",
  "license": "Apache-2.0 OR CC-BY-4.0",
  "description": "A table view from a JSON structure",
  "devDependencies": {
    "polymer-cli": "^0.16.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "private": false,
  "main": [
    "json-table.html"
  ],
  "author": "The Advanced REST client authors <arc@mulesoft.com>",
  "scripts": {
    "serve": "node_modules/.bin/polymer serve",
    "lint": "node_modules/.bin/polymer lint json-table.html",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"json-table:local-test\"",
    "test": "node_modules/.bin/polymer test",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/json-table.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/json-table/issues",
    "email": "arc@mulesoft.com"
  }
}