{
  "name": "request-editor",
  "version": "0.2.5",
  "license": "Apache-2.0 OR CC-BY-4.0",
  "description": "A request complete HTTP request editor",
  "devDependencies": {
    "bower": "^1.8.0",
    "polymer-cli": "^1.2.0"
  },
  "private": false,
  "main": [
    "request-editor.html"
  ],
  "author": "The Advanced REST client authors <arc@mulesoft.com>",
  "scripts": {
    "lint": "polymer lint request-editor.html -r dom-module-invalid-attrs -r style-into-template -r undefined-elements -r behaviors-spelling -r databinding-calls-must-be-functions -r element-before-dom-module -r unbalanced-polymer-delimiters",
    "test-sauce": "polymer test --plugin sauce --job-name \"request-editor:local-test\"",
    "test": "polymer test --plugin local",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/request-editor.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/request-editor/issues",
    "email": "arc@mulesoft.com"
  }
}