{
  "name": "headers-parser-behavior",
  "version": "0.1.4",
  "license": "Apache-2.0 OR CC-BY-4.0",
  "description": "Headers parser behavior to be impplemented with elements that needs to parse headers data",
  "devDependencies": {
    "polymer-cli": "^1.2.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "private": false,
  "main": [
    "headers-parser-behavior.html"
  ],
  "author": "jarrod",
  "scripts": {
    "lint": "node_modules/.bin/polymer lint headers-parser-behavior.html --rules polymer-1",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"headers-parser-behavior:local-test\"",
    "test": "node_modules/.bin/polymer test",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/headers-parser-behavior.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/headers-parser-behavior/issues"
  }
}