describe('TEST', () => {
  before(async () => {
    // const { ArcBindings } = await import('../../src/app/bindings/ArcBindings.js');
    // console.clear();
    console.log(document.body.outerHTML);
  });
  it('ok', () => {});
});
