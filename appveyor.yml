matrix:
  allow_failures:
    - nodejs_version: 8
    - nodejs_version: 10
install:
  - ps: Install-Product node $env:nodejs_version
  - set CI=true
  - npm install
build: off
version: '{build}'
shallow_clone: true
clone_depth: 1
test_script:
  - npm run test
